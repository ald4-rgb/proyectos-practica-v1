/* ingresar una fecha de nacimiento en formato string, convertirla a una
 * fecha del tipo java.util.Date y calcular la edad de la persona de acuerdo a la fecha actual
 * 
 */

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;

public class CalcularEdadUsandoDate {
    public static void main(String[] args) throws ParseException {

        //////////////ejemplito instructor
        
        // String ingresarFecha = "1995-10-11";

        // SimpleDateFormat dateCustom = new SimpleDateFormat("yyyy-MM-dd");

        // Date custom = dateCustom.parse(ingresarFecha);

        // Date actula = new Date();

        // dateCustom = new SimpleDateFormat("yyyyMMdd");

        // Integer desde = Integer.parseInt(dateCustom.format(custom));
        // Integer hoy = Integer.parseInt(dateCustom.format(actula));

        // int calculo = (hoy - desde) / 10000;

        // System.out.println(calculo);






    }

}
